# >=1.57 no es capricho: la app usa segmented_control, contenedores
# horizontales, width="stretch" y el formato compacto de NumberColumn.
streamlit>=1.57
pandas>=2.0
plotly>=5.20
# >=1.0 por `revenue_estimate` y `earnings_estimate`, que son de donde salen
# las estimaciones de consenso. Con una version anterior esas columnas quedan
# vacias y el diagnostico del Detalle lo dice.
yfinance>=1.0
requests>=2.31
certifi
XlsxWriter>=3.2
